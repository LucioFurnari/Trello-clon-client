import { useState } from "react"

interface UpdateWorkspaceProps {
  workspaceId: string
}

export default function EditWorkspace({ workspaceId }: UpdateWorkspaceProps) {
  return (
    <>
      <button>Edit</button>
    </>
  )
}